export interface IProduct {
   product_id: number
   name: string
   price: number
   image: string
   quantity: number
}

export interface handlePersonalInfoProps {
   target: {
      name: string
      value: string
   }
}
